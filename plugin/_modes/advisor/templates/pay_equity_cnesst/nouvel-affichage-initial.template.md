# Nouvel affichage — Exercice d'équité salariale (50+) — alias of `affichage-initial-final.template.md`

> This file is provided for naming alignment with CNESST terminology. The canonical template lives at `affichage-initial-final.template.md`. Both filenames render the same content via `render_document(client_slug, "nouvel_affichage_initial")`.
>
> Rationale: SKILL.md historically referenced `affichage-initial-final` as the document stem for the second 50+ posting. CNESST calls this the "nouvel affichage". Renaming the canonical file is deferred to R10 to avoid breaking existing references mid-rewrite.

See `affichage-initial-final.template.md` for the actual template content.
